server:
  port: 9999

# database config
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/baijing_security?useSSL=false&serverTimezone=Hongkong
    username: root
    password: root
  aop:
    proxy-target-class: true

# MyBatis config
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.lamlake.example.entity
